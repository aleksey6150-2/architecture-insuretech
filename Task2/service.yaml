apiVersion: v1
kind: Service
metadata:
  name: insuretech-test-app
  labels:
    app: insuretech-test-app
spec:
  type: ClusterIP
  selector:
    app: insuretech-test-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080